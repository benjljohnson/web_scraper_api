{
  "name": "web_scraper_api",
  "version": "1.0.0",
  "description": "A simple API for web scraping external links",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["web", "scraper", "api", "external", "links"],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "bull": "^3.29.3",
    "cheerio": "^1.0.0-rc.12"
    // Removed "request-promise" as it was not used in the provided code.
    // If it is used elsewhere in your code, you should add it back.
  }
}
